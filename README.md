# Project Structure

This repository is organized into three top-level areas:

- [frontend](frontend) for the Vite React application
- [backend](backend) for future API/server work
- [database](database) for Supabase migrations and database assets

To run the app locally, go into [frontend](frontend) and install dependencies there.